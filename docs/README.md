## Documentation

The generated documentation is built with Sphinx.

### Building

Install the requirements in requirements.txt.

In the `pydactyl/docs` directory run `make html`

Documentation will be generated in the `./build/html` dir.